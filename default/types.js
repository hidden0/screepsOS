// types.js
// @ts-check

/**
 * Valid creep role names for your codebase.
 * Add or remove strings as your strategy evolves.
 * TypeScript will now catch typos like 'upgarder'.
 * @typedef {'harvester' | 'upgrader' | 'builder' | 'defender'} RoleName
 */

// This file is only for types.  Export an empty object so it
// turns into a harmless module at runtime.
module.exports = {};
